@page "/add"

<PageTitle>Add</PageTitle>

<h1>Hello, world!</h1>
<form @onsubmit=add>
    <input @bind=name />
    <input type="submit" value=Add />
</form>

@code {
    string? name;
    public void add()
    {
        AttendanceManager.Instance.AddAttendee(name);
    }
}